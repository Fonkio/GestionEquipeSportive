Login : lapin
Mdp : canard

Tips :
On ne sait pas pourquoi, mais l'upload d'image fonctionne avec Laragon (MariaDB, apache2 et PHP 7.2.11), mais pas sur la version linux que l'on a installé (MariaDB, apache2 et PHP 5).
Le fichier lib.php contient toutes les fonctions dont nous avons besoin.
Le fichier BDD.sql est la base de donnée à importer (En cas de problème avec nacre) pour les tests en local.

